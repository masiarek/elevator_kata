mod building;
mod floor;
mod lift;
mod person;
mod tests;

use rand::Rng;
fn main() {
    println!("Hello Lift!");

    fn the_lift(queues: &[Vec<u32>], capacity: u32) -> Vec<u32> {
        vec![]
    }
}
